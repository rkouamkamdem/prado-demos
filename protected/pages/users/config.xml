<?xml version="1.0" encoding="utf-8"?>

<configuration>
    <authorization>
        <allow pages="NewUser" roles="admin" />
        <deny pages="*" users="?" />
        <!--  users="?" signifie utilisateur anonyme -->
    </authorization>
</configuration>